<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<include resource="org/springframework/boot/logging/logback/defaults.xml"/>
	<include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

	<!-- .env의 DISCORD_ERROR_WEBHOOK_URL. 비어있으면 DiscordWebhookAppender가 그냥 아무것도 안 보낸다 -->
	<springProperty name="DISCORD_WEBHOOK_URL" source="app.discord.error-webhook-url" defaultValue=""/>

	<appender name="DISCORD" class="com.baton.config.logging.DiscordWebhookAppender">
		<webhookUrl>${DISCORD_WEBHOOK_URL}</webhookUrl>
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>ERROR</level>
		</filter>
	</appender>

	<!-- 디스코드 전송이 느리거나 실패해도 애플리케이션 스레드가 안 멈추도록 비동기로 감싼다 -->
	<appender name="ASYNC_DISCORD" class="ch.qos.logback.classic.AsyncAppender">
		<appender-ref ref="DISCORD"/>
		<queueSize>256</queueSize>
		<includeCallerData>false</includeCallerData>
	</appender>

	<root level="INFO">
		<appender-ref ref="CONSOLE"/>
		<appender-ref ref="ASYNC_DISCORD"/>
	</root>
</configuration>
