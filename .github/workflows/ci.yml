name: CI

on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [develop, main]

jobs:
  build-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # DB/OpenAI 없이도 되는 범위만 검증한다. 실제 기동 확인은 로컬/서버에서 직접 한다.
      - name: Compile
        run: ./gradlew compileJava compileTestJava --no-daemon

      - name: AI analysis unit tests
        run: >-
          ./gradlew test
          --tests 'com.baton.ai.AnalysisJobTest'
          --tests 'com.baton.ai.AnalysisJobServiceTest'
          --tests 'com.baton.ai.dto.QuestionAnswerRequestTest'
          --tests 'com.baton.handover.HandoverAnalysisStateTest'
          --no-daemon

      - name: AI usage limit unit tests
        run: >-
          ./gradlew test
          --tests 'com.baton.aiusage.AiUsageGuardTest'
          --tests 'com.baton.aiusage.AiTaskLockServiceTest'
          --tests 'com.baton.ai.RagAnalysisServiceCompleteTest'
          --no-daemon

      - name: Masking review unit tests
        run: >-
          ./gradlew test
          --tests 'com.baton.masking.MaskingDetectorTest'
          --tests 'com.baton.masking.MaskingCandidateTest'
          --tests 'com.baton.masking.MaskingServiceTest'
          --tests 'com.baton.masking.MaskingApplierTest'
          --tests 'com.baton.masking.MaskingConfirmationTest'
          --tests 'com.baton.ai.RagIngestServiceConfirmTest'
          --tests 'com.baton.ai.RagIngestServiceMaskingTest'
          --tests 'com.baton.ai.RagIngestServiceQuotaTest'
          --no-daemon

      - name: PDF evidence unit tests
        run: >-
          ./gradlew test
          --tests 'com.baton.ai.PdfEvidenceTest'
          --tests 'com.baton.ai.EvidenceAccessTest'
          --tests 'com.baton.masking.MaskingEvidenceTest'
          --tests 'com.baton.readiness.ReadinessItemNormalizerTest'
          --tests 'com.baton.readiness.ReadinessFixTest'
          --no-daemon
